@model testAuten.Models.Produto

@{
    ViewData["Title"] = "Create";
}

<h1>Criar Novo</h1>

<partial name="_SubtitlePageList" model='new SubtitleViewModel { Title = "Produto", ButtonValue = "Voltar para a Lista" }' />
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Nome" class="control-label"></label>
                <input asp-for="Nome" class="form-control" />
                <span asp-validation-for="Nome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Preco" class="control-label"></label>
                <input asp-for="Preco" class="form-control" id="precoProd" />
                <span asp-validation-for="Preco" class="text-danger"></span>
            </div>
            <div class="form-group" style="margin-top:1rem;">
                <input id="saveProd" type="submit" value="Guardar" class="btn btn-outline-success" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script>
        // jQuery('#saveProd').on('click', function(){
        //     debugger;
        //     const precoInput = jQuery('#precoProd');
        //     let precoValue = precoInput.val();

        //     precoValue = precoValue.replace('.', ',');
        //     precoInput.val(precoValue);
        // });
    </script>
}
